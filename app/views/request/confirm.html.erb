<div class="container mt30 mb100 YuGothic">

  <h3>こんにちは、<%= @user.name %>さん</h3>

  <h3>家族留学の申し込み内容は以下の通りです。</h3>

  <table id="table-info" class="table table-bordered table-striped">

    <tbody>
    <tr>
      <th>名前</th>
      <td><%= @log.name %></td>
    </tr>
    <tr>
      <th>所属</th>
      <td><%= @log.belongs %></td>
    </tr>
    <% @days.each_with_index do |day, i| %>
        <tr>
          <th>参加希望日(<%= i + 1 %>)</th>
          <td><%= day.day %></td>
        </tr>
    <% end %>
    <tr>
      <th>最寄駅</th>
      <td><%= @log.station %></td>
    </tr>
    <tr>
      <th>希望理由</th>
      <td><%= @log.motivation %></td>
    </tr>
    </tbody>
  </table>
  <p class="fs23">家族留学を受け入れますか？（家族留学実施日の時間変更は可能です）</p>
  <%= link_to '受け入れる', reply_path(params[:id], email: params[:email]), :class => 'btn btn-lg bg-pink btn-block' %>
  <%= link_to '今回は辞退する', deny_path(log_id: @log.id, email: params[:email]), :class => 'btn btn-lg btn-default btn-block' %>
</div>



