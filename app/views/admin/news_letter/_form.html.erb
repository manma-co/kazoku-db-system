<table>
  <tr>
    <td><%= f.label :subject, '件名: ' %></td>
    <td><%= f.text_field :subject, required: true %></td>
  </tr>
  <!-- TODO: add date picker -->
  <tr>
    <td><%= f.label :distribution, '送信日時: ' %></td>
    <td><%= f.text_field :distribution, required: true %></td>
  </tr>
  <tr>
    <td><%= f.label :send_to, '送信先: ' %></td>
    <td><%= f.select :send_to, {'参加者' => 'participant', '家庭' => 'family'}, { :include_blank => '-- 選択してください --' }, required: true %></td>
  </tr>
  <tr>
    <td><%= f.label :content, '本文: ' %></td>
    <td><%= f.text_area :content, rows: 10, class: 'h-300', required: true %></td>
  </tr>
  <tr>
    <td><%= f.label :is_save, '下書き: ' %></td>
    <td><%= f.check_box :is_save, class: 'checkbox' %></td>
  </tr>
</table>
